#include "header.h"

int optimation(char **table)
{
    int i;
    struct Pos *pos;
    pos->x = 0;
    pos->y = 0;
    i = 1;
    while (table[pos.y] && table[pos.x])
    {
        pos->w = pos->x + 1;
        pos->h = pos->y + 1;
    } 
    
    return (1);
}